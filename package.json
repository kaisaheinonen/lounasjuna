{
  "name": "lounasjuna",
  "version": "1.0.0",
  "description": "Toimiston lounasjuna - prototyyppi",
  "scripts": {
    "backend": "cd backend && node server.js",
    "frontend": "cd frontend && npm run dev",
    "install:all": "cd backend && npm install && cd ../frontend && npm install"
  }
}
